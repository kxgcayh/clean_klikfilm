import 'package:flutter/material.dart';

class KidsModePage extends StatelessWidget {
  const KidsModePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('KidsModePage is Working'),
      ),
    );
  }
}
