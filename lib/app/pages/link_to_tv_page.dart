import 'package:flutter/material.dart';

class LinkToTvPage extends StatelessWidget {
  const LinkToTvPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('LinkToTvPage is Working'),
      ),
    );
  }
}
